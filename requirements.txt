streamlit>=1.35.0
protobuf>=3.20,<4
requests>=2.31.0
python-docx>=1.1.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
pyinstaller>=6.0.0
# Windows auto-update: detects a newer GitHub Release on startup and applies it
# in place (per-user install, no admin). Only active in the frozen build; the
# update check is a no-op in dev / when not installed via the Velopack Setup.exe.
velopack>=0.0.1051
# Optional: only used when TEMPLATE_SOURCE=sharepoint, to reuse the user's existing
# browser sign-in so templates can be fetched without a stored credential. The app
# runs fine without it (it just falls back to bundled templates).
browser-cookie3>=0.19.1
